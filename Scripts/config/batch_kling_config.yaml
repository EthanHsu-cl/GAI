template_path: templates/I2V templates.pptx
comparison_template_path: templates/I2V Comparison Template.pptx
output_directory: /Users/ethanhsu/Desktop/EthanHsu-cl/GAI/Report
comment_output_directory: 'Optional: Global output directory for all generated PPTX files. Can be overridden per task. Default
  is current directory.'
testbed: http://192.168.31.161/external-testbed/kling/
model_version: v2.5-turbo
comment_model_version: 'Model version to use for video generation. Options: ''v1.6'', ''v2.1''. Default is ''v2.1'' if not
  specified.'
schedule:
  start_time: ''
  comment: 'Time format: HH:MM (24-hour format). Leave empty to start immediately'
group_tasks_by: 4

tasks:
- mode: std
  folder: Media Files/Kling 2.5/0518 3 Effects/Police
  prompt: |
    she wear police uniform shirt and wear sunglasses in the same time. the main character stands confidently under a spotlight. Without taking a step, their outfit instantly transforms in place into a sleek, crisp blue police uniform. They smoothly put on a pair of black sunglasses, followed by placing a police hat on their head with precision. In one fluid motion, the character draws a handgun and strikes a dramatic pose. they quickly begin firing multiple precise shots in different directions. Each shot emits bright muzzle flashes that briefly light up the room. The scene is shot in a cinematic slow-motion style with realistic textures, subtle light reflections on the sunglasses, and sharp camera focus, highlighting the stylish and powerful transformation.
  negative_prompt: ''
  design_link: ''
  source_video_link: ''
  reference_folder: ''
  use_comparison_template: false

- mode: std
  folder: Media Files/Kling 2.5/0518 3 Effects/Mystic Fox
  prompt: |
    The subject places both hands on their waist and performs rhythmic movements, especially focusing on hip twisting.
    Requirements:
    1. If there are multiple subjects in the image, all of them must perform the hip-twisting motion described above.
    2. If the subject is a pet, it should be anthropomorphized to perform the hip-twisting motion.
  negative_prompt: ''
  design_link: ''
  source_video_link: ''
  reference_folder: ''
  use_comparison_template: false

- mode: std
  folder: Media Files/Kling 2.5/0518 3 Effects/Bubbles
  prompt: |
    A dreamy cinematic scene with the main character in the center (e.g., a girl, a fairy, a cat, etc.), joyfully looking around at the magical, beautiful bubbles surrounding her. She raises both hands with palms facing up, expressing wonder and happiness. Her facial expression is a simple open-mouth smile (no lip movement), conveying delight without looking like she's speaking. The bubbles are translucent and shimmer with rainbow reflections as they gently drift in the air. The character remains fully visible and centered in the frame at all times. Soft lighting, bokeh background, magical atmosphere, smooth camera motion, elegant and stable composition.
  negative_prompt: ''
  design_link: ''
  source_video_link: ''
  reference_folder: ''
  use_comparison_template: false

comments:
  model_version: |
    Model version to use for video generation.
    Options: 'v1.6', 'v2.1', 'v2.5-turbo'
    Default: 'v2.1' if not specified.
    Can be set globally or per-task.
  mode: |
    Generation mode for video quality.
    Options: 'std' (standard, faster) or 'pro' (professional, higher quality)
    Default: 'std'
  duration: |
    Video duration in seconds.
    Options: 5, 10
    Default: 5
  cfg: |
    CFG scale (Classifier Free Guidance).
    Range: 0.0 - 1.0
    Higher values = more adherence to prompt, less creative freedom.
    Default: 0.5
  prompt: |
    Text description of desired video motion and content.
    Should describe the action, movement, and visual style.
  negative_prompt: |
    Elements to avoid in the generated video.
    Helps prevent unwanted artifacts or style elements.
  design_link: |
    URL to design reference document (e.g., Google Slides, Figma).
    Used for report generation and reference.
  source_video_link: |
    URL to source video files (e.g., OneDrive, Google Drive).
    Used for report generation and reference.
  reference_folder: |
    Path to folder containing reference images for comparison.
    Optional - used when use_comparison_template is true.
  use_comparison_template: |
    Set to true to use comparison_template_path (3 placeholders: source, reference, output).
    Set to false to use template_path (2 placeholders: source, output).
  group_tasks_by: |
    Set to 0 (or omit) to create individual reports per task.
    Set to N to group N tasks into one combined report.
    Example: with 8 tasks and group_tasks_by=4, creates 2 reports.
  schedule: |
    start_time: HH:MM (24-hour format). Leave empty to start immediately.
    Allows delayed start for batch processing.
  folder_structure: |
    Each task folder should have 'Source', 'Generated_Video', and 'Metadata' subfolders.
    Source folder contains input images to be animated.

